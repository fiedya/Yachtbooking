type WebDatePickerProps = {
  value: Date;
  onChange: (date: Date) => void;
  mode: "date" | "time";
  placeholder?: string;
};

export default function WebDatePicker(_props: WebDatePickerProps) {
  return null;
}
